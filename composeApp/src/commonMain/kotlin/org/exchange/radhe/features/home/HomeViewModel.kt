package org.exchange.radhe.features.home

import cafe.adriel.voyager.core.model.ScreenModel

/**
 * ViewModel for the Home feature.
 *
 * Currently acts as a placeholder as most logic has been moved to persistent background services
 * (like `WebSocketService` on Android) to ensure reliability.
 */
class HomeViewModel : ScreenModel {
    // Logic is primarily handled by the platform-specific background services.
}
